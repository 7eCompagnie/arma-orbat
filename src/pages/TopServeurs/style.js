import {createStyles} from "@mantine/core";

export const useStyles = createStyles((theme, icon_size) => ({
	value: {
		textAlign: "center",
		width: '100%',
		fontWeight: 700,
		lineHeight: 1,
	},

	title: {
		fontWeight: 700,
		fontSize: 14,
		textAlign: 'center',
		width: '100%'
	},
}));
